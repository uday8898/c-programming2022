#include <stdio.h>
int main()
{
    int a, b;
    printf("Enter the number---> ");
    scanf("%d", &a);

    printf("ENter the second number---> ");
    scanf("%d", &b);

    printf("The sum of the two input numbers is---> %d", a+b);

    return 0 ;
}